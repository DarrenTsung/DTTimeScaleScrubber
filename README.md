# DTTimeScaleScrubber

Small-scaled debugging unity script that allows for slowing down and speeding up time. 

Default uses Q - E keys, two-finger touch, or InControl controller shoulder buttons by default. 
